<div id="header">
  <%#= link_to image_tag("logo.png", :alt => "Logo"), '/', :class => 'logo' -%>
  <div class='logo'>LOGO</div>
  <% if user_signed_in? %>
    <ul class='user_menu'>
      <li><%= current_user.name %></li>
      <li class="last"><%= link_to("Sign Out", destroy_user_session_path, :class => "logout") %></li>
    </ul>
  <% end %>
</div>